domain: ''

secret_key: 'asfdlnan3o49rj94jofewmcdsozixjp90womed,sa[0op'

jdbc:
  name: 'transportik_db'
  host: 'localhost'
  port: 5432
  user: 'transportik_user'
  password: 'transportik_pass'
  engine: 'django.contrib.gis.db.backends.postgis'
  conn_max_age: 60

jwt:
  verify_token: true
  verify_expiration: true
  expiration_time: 3000
  header: 'Bearer'

cassandra:
  contact_points: ['ec2-18-208-176-172.compute-1.amazonaws.com']
  port: 9042
  keyspace: 'default'
  table_name: 'transport_analysis'